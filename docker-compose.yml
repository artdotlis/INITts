version: '3'
services:
    deploy:
        build:
            context: .
            dockerfile: ./Dockerfile
        volumes:
            - ./configs/dev/nginx.conf:/etc/nginx/conf.d/default.conf:ro
        ports:
            - 8086:80
